// chain-api using polkadot.js


// ......




// -------------------------------
function datdot_chain (name, list, wire) {
  const { from, send, protocol } = wire('datdot-chain')
  const wire1 = protocol('helper1')
  helper1(name, list, wire1)
}
  from('help1')(message => {

  })
  send('help1', 'start')

// // -------------------------------
// function datdot_chain (name, list, wire) {
//
//     const { from, send, protocol } = wire('datdot-chain')
//
//     const wire1 = protocol('helper1')
//
//     helper1(name, list, wire1)
// }
