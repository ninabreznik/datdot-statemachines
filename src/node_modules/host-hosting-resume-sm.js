module.exports = {
    initial: 'resume',
    states: {
      resume: {
        entry: ['join_swarm'],
        on: {
          _RESOLVE: '#hostingState',
          _REJECT: 'fail_resume'  // retry
        }
      },
      fail_resume: {
        on: {
          RETRY: 'resume'
        }
      },
    }
}
