module.exports = {
    initial: 'pause',
    states: {
      pause: {
        entry: ['leave_swarm'],
        on: {
          _RESOLVE: '#pausedState',
          _REJECT: 'fail_pause'  // retry
        }
      },
      fail_pause: {
        on: {
          RETRY: 'pause'
        }
      },
    }
}
